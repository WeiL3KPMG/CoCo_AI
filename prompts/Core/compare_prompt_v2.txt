You are comparing one candidate company with one target company for comparable-company screening.
Output ONLY one valid JSON object. Do not output markdown. Do not output code fences.

Scoring objective:
- Score similarity to the target company (not absolute company quality).
- Use only evidence in the provided target text and candidate fields.
- Do not invent facts not present in the input.

Critical discipline rules:
- Keep criterion labels exactly as specified.
- Use ONLY the allowed score bands below (no other numeric values).
- overall_score must equal the exact sum of criterion score values.
- Assign tier strictly from overall_score:
  - Strong: 70-100
  - Median: 40-69
  - Weak: 0-39
- tier must be exactly one of: Strong, Median, Weak.
- Each criterion reason should be concise (typically 1-3 sentences) and include:
  - strongest explicit evidence supporting the selected band, and
  - key gap or difference when relevant.
- Special case for score 0:
  - clearly explain the non-overlap that makes comparability minimal for that criterion.
- Do not infer geography solely from ticker or listing exchange.
- Do not infer scale from brand familiarity, reputation, or assumed market prominence.

IMPORTANT CALIBRATION (to prevent trivial all-zero outputs):
- Score 0 is reserved for FUNDAMENTALLY UNRELATED situations for that criterion.
  Examples of "fundamentally unrelated":
  - Different industries with no shared value-chain adjacency (e.g., consumer retail vs utility generation).
  - Business models that cannot be compared within the criterion scope (e.g., pure entertainment studio vs power infrastructure owner).
- If companies are in the SAME BROAD ECOSYSTEM (e.g., energy, power, infrastructure, industrial services), then avoid assigning 0 unless the criterion is truly non-overlapping.
  In these adjacency cases:
  - Prefer the LOWEST NON-ZERO band for that criterion when evidence is limited,
  - Set evidence_strength to low,
  - Clearly state what explicit evidence is missing in the reason.

Decision logic (balanced):
- If the criterion has SOME explicit adjacency signal but details are sparse, prefer a low non-zero band and set evidence_strength=low.
- If evidence plausibly supports two adjacent allowed bands, choose the best-fit band and explain uncertainty in the reason.
- Use midpoint bands when overlap is material but incomplete; reserve top bands for clearly strong alignment.
- Only assign 0 when there is no meaningful overlap AND the companies are fundamentally unrelated for that criterion.

Criterion scope boundaries (to reduce double counting):

1) Business Model & Activities (max 40)
Allowed scores: 0, 10, 20, 30, 40
Consider ONLY: operating workflow, value-chain role, revenue model, ownership/operator/developer profile.
Do NOT consider: geography, scale, macro strategy themes unless directly embedded in operating model.

Band guidance:
- 40: Core operating workflow and value-chain role are highly similar; primary revenue logic closely aligned.
- 30: At least one core workflow or value-chain role overlaps, but main revenue concentration or operating profile differs.
- 20: Broad adjacency with partial workflow overlap; meaningful operating-model gaps remain.
- 10: Limited workflow overlap; mostly indirect adjacency within the same broad ecosystem.
- 0: Fundamentally unrelated operating models with no value-chain adjacency.

Adjacency note (Business Model):
- Operator/developer/owner of generation & storage vs service/contractor/consultancy to energy projects is usually 10–20, not 0, if explicitly energy/infrastructure-linked.

2) Strategic & Sector Alignment (max 25)
Allowed scores: 0, 6, 12, 18, 25
Consider ONLY: end-market exposure, sector positioning, strategic themes, material risk/opportunity profile.
Do NOT consider: direct workflow similarity, geography, scale.

Band guidance:
- 25: End-markets, strategic positioning, and key sector themes strongly aligned.
- 18: Strong alignment with clear but not dominant differences.
- 12: Moderate alignment; meaningful differences in sector positioning or strategic direction.
- 6: Limited alignment; partial common themes in the same broad ecosystem.
- 0: Fundamentally unrelated sectors with no shared end-market or themes.

Adjacency note (Strategic):
- Oil & gas services vs renewable power infrastructure: typically 6 (shared energy/infrastructure context) unless the candidate has no credible energy/infrastructure exposure in the provided text.
- Pure consulting across unrelated sectors: can be 0 if no energy/infrastructure end-market exposure is explicitly indicated.

3) Scale & Infrastructure Intensity (max 20)
Allowed scores: 0, 5, 10, 15, 20
Consider ONLY: explicit indicators of operational scale, asset/infrastructure intensity, infrastructure role.
Do NOT consider: company fame, perceived size, or unsupported assumptions.

Band guidance:
- 20: Similar scale profile and similar infrastructure/asset intensity.
- 15: Broadly similar with moderate scale/intensity differences.
- 10: Partial comparability; noticeable scale/intensity gap.
- 5: Limited comparability; major gap but still within infrastructure/asset-heavy context.
- 0: Fundamentally different scale/intensity profile (e.g., asset-light professional services vs asset-heavy infrastructure) with no comparable infrastructure role.

Adjacency note (Scale/Intensity):
- If the candidate is clearly project/infrastructure-heavy (even in a different sub-sector), use 5 rather than 0 when explicit signals exist.
- If the candidate is asset-light consulting-only and no asset intensity indicators exist, 0 may be appropriate.

4) Geography Relevance (max 15)
Allowed scores: 0, 5, 10, 15
Consider ONLY: explicit operating regions, geographic footprint, and market exposure in provided text.
Do NOT infer geography from listing exchange, ticker, or company name.

Band guidance:
- 15: Highly aligned geographic footprint and market exposure.
- 10: Good overlap with meaningful regional mismatch.
- 5: Limited overlap (some shared regions explicitly stated).
- 0: Minimal or no geographic overlap based on explicit text.

Evidence strength rules:
- high: direct explicit evidence in input clearly supports selected band.
- medium: partial or indirect evidence supports selected band.
- low: sparse/ambiguous evidence or key details are missing (including “adjacent but underspecified” cases).

Borderline decision definition:
- A criterion is borderline when available evidence could reasonably fit two adjacent allowed score bands.
- Track borderline decisions internally for confidence calculation (do NOT output a borderline field).

Confidence rules (must be exactly one of: high, medium, low)
Guidance-based mapping:
1) Count:
   - low_count = number of criteria where evidence_strength = low
   - borderline_count = number of criteria that are borderline decisions
   - high_count = number of criteria where evidence_strength = high
2) Set confidence = low when evidence is materially incomplete or highly ambiguous (e.g., low_count >= 2 OR borderline_count >= 2).
3) Set confidence = high when evidence is clear and consistent across most criteria (typically high_count >= 3 and borderline_count = 0).
4) Otherwise set confidence = medium.

Low-evidence aggregation rule:
- If low_count >= 2:
  - confidence should not be high, and
  - tier_justification should mention limited/insufficient evidence.

tier_justification rules:
- Should be 2-4 sentences.
- Include: strongest comparability basis, main limiting difference(s), and why the total score fits the assigned tier.
- If low_count >= 2, mention limited/insufficient evidence.

key_differences_vs_target rules:
- Output 1 to 3 short strings.
- Each must be non-empty and describe one concrete material difference affecting comparability.
- Do not repeat the same point with different wording.

Output JSON schema (must match field names and criterion order):
{
  "company_name": "",
  "ticker": "",
  "overall_score": 0,
  "tier": "Strong",
  "tier_justification": "",
  "criteria_scores": [
    {
      "criteria": "Business Model & Activities",
      "max_score": 40,
      "score": 0,
      "reason": "",
      "evidence_strength": "high"
    },
    {
      "criteria": "Strategic & Sector Alignment",
      "max_score": 25,
      "score": 0,
      "reason": "",
      "evidence_strength": "medium"
    },
    {
      "criteria": "Scale & Infrastructure Intensity",
      "max_score": 20,
      "score": 0,
      "reason": "",
      "evidence_strength": "low"
    },
    {
      "criteria": "Geography Relevance",
      "max_score": 15,
      "score": 0,
      "reason": "",
      "evidence_strength": "high"
    }
  ],
  "key_differences_vs_target": [
    "Different end-market concentration and customer mix."
  ],
  "confidence": "medium",
  "rubric_version": "v2_banded_balanced_1"
}

Validation checklist before final output:
- Exactly 4 criteria in the specified order.
- Each criterion score uses only that criterion's allowed band values.
- overall_score equals the sum of the 4 criterion scores.
- tier matches score band rule exactly.
- key_differences_vs_target has 1-3 non-empty strings.
- confidence is exactly one of high, medium, low.
- confidence follows the guidance-based mapping rule.
- If low_count >= 2, avoid high confidence and mention limited/insufficient evidence in tier_justification.
- Output is a valid JSON object only.

Target Company Reference:
{PASTE TARGET TXT HERE}

Candidate Company:
{PASTE CANDIDATE JSON HERE}
